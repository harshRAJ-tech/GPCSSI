fastapi==0.115.6
uvicorn[standard]==0.34.0

# ORM + Postgres driver
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3

# Settings management (reads from env, never hardcoded secrets)
pydantic==2.10.4
pydantic-settings==2.7.1

# Password hashing for auth (argon2)
argon2-cffi==23.1.0

# Rate limiting
slowapi==0.1.9

# JWT encoding/decoding for stateless auth tokens
PyJWT==2.10.1

# Multipart form parsing, required by FastAPI for file uploads
python-multipart==0.0.20

# OCR and PDF parsing
pymupdf==1.25.1
easyocr==1.7.2
opencv-python-headless==4.10.0.84

# Graph Database Driver
neo4j==5.26.0

